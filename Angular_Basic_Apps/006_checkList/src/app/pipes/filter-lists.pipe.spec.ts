import { FilterListsPipe } from './filter-lists.pipe';

describe('FilterListsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListsPipe();
    expect(pipe).toBeTruthy();
  });
});
